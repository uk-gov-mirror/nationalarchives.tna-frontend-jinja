[project]
name = "tna-frontend-jinja-django-example"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
  "Django>=5.2,<6",
  "tna-frontend-jinja[django]",
]

[tool.pytest.ini_options]
pythonpath = ["app"]